<!DOCTYPE html>
<html lang="en">
<head>
  <title>Lifetime Supply Calculator</title>
</head>
<body>
  <h1>Lifetime Snack Supply Calculator</h1>

  <script>
    let age = 14; 
    let maxAge = 90;
    let numPerDay = 2; 

    let totalRequired = (numPerDay * 365) * (maxAge - age);

    document.write(""You will need " + totalRequired + " to last you until the ripe old age of " + maxAge + ."");
      
  </script>
</body>
</html>

